$(document).ready(function(){
    let name = $("#Username").value;
    let pass = $("#Password").value;

    let userList = localStorage.getItem(localStorage.key(0))
    for (let i = 0; i < userList.length; i++)
    {
        if (name == userList[i].)
    }

})